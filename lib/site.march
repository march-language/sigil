mod Site do

  -- Site-wide metadata available to every layout call.
  type Info = {
    title       : String,
    base_url    : String,
    author      : String,
    description : String
  }

  -- Default site info with empty strings — override in your main.march.
  fn default() : Info do
    { title = "My Site", base_url = "", author = "", description = "" }
  end

end
